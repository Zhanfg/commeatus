# Tests

Test layout:

- `compatibility/` — ecosystem compatibility tests (mihomo, sing-box,
  Clash) exercised against the compatibility boundary
- `regression/` — regression tests, run on every change
- `stability/` — long-running stability and soak tests
- `benchmark/` — performance and idle-power benchmarks

Bootstrap stage: no tests yet.
