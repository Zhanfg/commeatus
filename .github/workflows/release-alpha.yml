name: v0.1 release archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: v0.1.0-alpha.1 is already published
    runs-on: ubuntu-latest
    steps:
      - name: Historical release workflow
        run: |
          echo "v0.1.0-alpha.1 was published on 2026-08-15."
          echo "This workflow is retained as history and performs no publication."
