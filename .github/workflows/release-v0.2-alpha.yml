name: v0.2 release archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    name: v0.2.0-alpha.1 is already published
    runs-on: ubuntu-latest
    steps:
      - name: Historical release workflow
        run: |
          echo "v0.2.0-alpha.1 is already published."
          echo "This workflow is retained as history and performs no publication."
