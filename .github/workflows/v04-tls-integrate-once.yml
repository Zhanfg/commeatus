name: v0.4 one-shot TLS integration

on:
  push:
    branches:
      - feat/v0.4-tls-transport

permissions:
  contents: write

concurrency:
  group: v04-tls-integrate-once
  cancel-in-progress: true

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: feat/v0.4-tls-transport
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@4360b52568e2003a75bf9bc1d59f33a8e3fc893c
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Integrate TLS config and tests
        run: |
          python3 scripts/v04_tls_integrate.py
          cargo update
          cargo fmt --all
          cargo check --workspace --all-targets
          cargo clippy --workspace --all-targets -- -D warnings
          cargo test --workspace
      - name: Commit verified integration and remove temporary files
        run: |
          rm .github/workflows/v04-tls-integrate-once.yml
          rm scripts/v04_tls_integrate.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "feat(v0.4): expose TLS transport in native endpoints"
          git push origin HEAD:feat/v0.4-tls-transport
